schema {
  mutation: Mutation
  query: Query 
}
type Mutation {
  login(email: String!, password: String!): UserResponse
  register(input: RegisterInput): RegisterInputResponse
  logout: LogoutResponse
}

type Query {
  getUser(_id: String!): User
}
