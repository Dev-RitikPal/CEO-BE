schema {
  mutation: Mutation
  query: Query 
}
type Mutation {
  login(email: String!, password: String!): UserResponse
  signup(input: CreateUserInput): CreateUserInputResponse
}

type Query {
  getUser(_id: String!): User
}
